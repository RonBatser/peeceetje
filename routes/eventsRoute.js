/* * this file contains all routes regarding the events */const showPages = require("../util/showPages");const userFunctions = require("../util/usersFunctions");module.exports = function (app) {    app.get(["/Events", "/events"], function (req, res) {        userFunctions.getCurrentUser(req, res, function () {            userFunctions.ensureUserVerification(req, res, function () {                res.render("events.ejs");            });        });    });    app.get(["/FacebookEvents", "/facebookevents", "/Facebookevents", "/facebookEvents"], function (req, res) {        showPages.loadEventsPage(req, res);    });    app.get(["/Facebooklocation", "/facebooklocation"], function (req, res) {        showPages.loadLocation(req, res);    });};