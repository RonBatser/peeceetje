<nav class="navbar navbar-expand-lg navbar-light">
    <a class="navbar-brand" href="/">
        <img alt="Logo" src="/assets/media/logo/logo.svg">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <i class="fa fa-2x fa-bars"></i>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link<% if (this.activeNavItem && this.activeNavItem === 'home') { %> active<% } %>"
               href="/">
                <i class="fa fa-fw fa-qrcode"></i> Home
                <% if (this.activeNavItem && this.activeNavItem === 'home') { %><span class="sr-only">(current)</span>
                <% } %>
            </a>
            <a class="nav-item nav-link<% if (this.activeNavItem && this.activeNavItem === 'events') { %> active<% } %>"
               href="/Events">
                <i class="fa fa-fw fa-calendar-alt"></i> Evenementen
                <% if (this.activeNavItem && this.activeNavItem === 'events') { %><span class="sr-only">(current)</span>
                <% } %>
            </a>
            <% if (locals.currentUser && (currentUser.role === 'admin' || currentUser.role === 'barman')) { %>
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false">
                        <i class="fa fa-fw fa-database"></i> Beheer
                    </a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item<% if (this.activeNavItem && this.activeNavItem === 'cashdesk') { %> active<% } %>"
                           href="/Cashdesk">
                            <i class="fa fa-fw fa-shopping-cart"></i> Kassa
                            <% if (this.activeNavItem && this.activeNavItem === 'cashdesk') { %><span
                                    class="sr-only">(current)</span>
                            <% } %>
                        </a>
                        <a class="dropdown-item<% if (this.activeNavItem && this.activeNavItem === 'inventaris') { %> active<% } %>"
                           href="/Inventaris">
                            <i class="fa fa-fw fa-calculator"></i> Inventaris
                            <% if (this.activeNavItem && this.activeNavItem === 'inventaris') { %><span class="sr-only">(current)</span>
                            <% } %>
                        </a>
                        <% if (currentUser.role === 'admin') { %>
                            <a class="dropdown-item<% if (this.activeNavItem && this.activeNavItem === 'members') { %> active<% } %>"
                               href="/Users">
                                <i class="fa fa-fw fa-users"></i> Leden
                                <% if (this.activeNavItem && this.activeNavItem === 'members') { %><span
                                        class="sr-only">(current)</span>
                                <% } %>
                            </a>
                            <a class="dropdown-item<% if (this.activeNavItem && this.activeNavItem === 'appsettings') { %> active<% } %>"
                                href="/appsettings">
                                 <i class="fa fa-fw fa-cog"></i> App instellingen
                                 <% if (this.activeNavItem && this.activeNavItem === 'appsettings') { %><span
                                         class="sr-only">(current)</span>
                                 <% } %>
                             </a>
                        <% } %>
                    </div>
                </div>
                <a hidden href="#" class="nav-item nav-link show-cache-button" data-toggle="modal"
                   data-target="#modal-storage-items">
                    <i class="fa fa-fw fa-cloud"></i> Sync
                </a>
            <% } %>
        </div>
        <div class="navbar-nav ml-auto">
            <div class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-haspopup="true"
                   aria-expanded="false">
                    <i class="fa fa-fw fa-cog"></i><% if (locals.currentUser) { %><span class="lg-only"> <%= currentUser.username %><% } %></span>
                </a>
                <div class="dropdown-menu dropdown-menu dropdown-menu-right">
                    <a class="dropdown-item<% if (this.activeNavItem && this.activeNavItem === 'profile') { %> active<% } %>"
                       href="/Profile">
                        <i class="fa fa-fw fa-user"></i> Accountinstellingen
                        <% if (this.activeNavItem && this.activeNavItem === 'profile') { %><span class="sr-only">(current)</span>
                        <% } %>
                    </a>
                </div>
            </div>
            <a class="nav-item nav-link" href="/LogOut"><i class="fa fa-fw fa-sign-out-alt"></i> Afmelden</a>

        </div>
    </div>
</nav>
